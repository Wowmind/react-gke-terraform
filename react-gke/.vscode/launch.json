{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/react-gke-app/package.json",
                "localRoot": "${workspaceFolder}/react-gke-app"
            }
        }
    ]
}